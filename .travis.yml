language: r

# apt_packages does not work here
addons:
  apt:
    packages:
      - libarpack2-dev

cache:
 - packages
 - directories:
    - process/idmap/cache

install:
 - make deps
 - make -C stats nmf_mu.so

script: make test
